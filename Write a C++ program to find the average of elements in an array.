#include<iostream>
using namespace std;
int main()
{
  int a[3]={1,2,3};
  int avg,sum=0,l,i;
  l=sizeof(a)/sizeof(a[1]);
  for(i=0;i<l;i++)
    sum=sum+a[i];             
  avg=sum/l;
  cout<<"The sum of average is: "<<avg;
  return 0;
}
