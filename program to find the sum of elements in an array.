#include<stdio.h>
#include<iostream>
using namespace std;
int main()
{
int arr [5]={3,5,6,7,9};
int i,m=0,x,n;
  x=sizeof(arr)/sizeof(arr[0]);
  for(i=0;i<x;i++)
  {
    m=m+arr[i];
  cout<<arr[i]<<"\n";
  }
cout<<"the sum of the array is "<<m<<"\n";
cout<<n;
  
}
