#include<iostream>
using namespace std;
int main()
{
  int a[]={1,2,3,4,5};
  int n,i,b[100],e;
 cout<<"Enter the position\n";
    cin>>n;
  e=sizeof(a)/sizeof(a[0]);
   for(i=0;i<n;i++)
      b[i]=a[i];
  for(i=n;i<e-1;i++)
    b[i]=a[i+1];
  for(i=0;i<e-1;i++)
    cout<<b[i]<<" ";
  
}
