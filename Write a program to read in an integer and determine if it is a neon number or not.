#include<iostream>
using namespace std;
int main()
{
int a,b,n,m,s=0;
  cout<<"Enter a number\n";
  cin>>a;
  n=a*a;
  while(n>0)
  {
    m=n%10;
    s=s+m;
    n=n/10;
  }
  if(a==s)
    cout<<a<<" is a neon number";
  else
    cout<<a<<" is not a neon number";
}
